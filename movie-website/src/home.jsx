import Search from "./Search"
import Movie from "./Movie"
export default function Home(){
   // const name=useContext(AppContext);
    return(
<div>
   <Search/> 
   <Movie/> 
</div>   //we have got the data from the ware house

)
}